import colorsLight from '../scss/colors-light.exports.scss';

export const variables = {
  navy: {
    darker: colorsLight.navyDarker,
    dark: colorsLight.navyDark,
    default: colorsLight.navy,
    light: colorsLight.navyLight,
    lighter: colorsLight.navyLighter,
    //lightest: colorsLight.navyLightest,
    white: colorsLight.navyWhite,
  },

  blue: {
    darker: colorsLight.blueDarker,
    dark: colorsLight.blueDark,
    default: colorsLight.blue,
    light: colorsLight.blueLight,
    lighter: colorsLight.blueLighter,
  },

  orange: {
    darker: colorsLight.orangeDarker,
    dark: colorsLight.orangeDark,
    default: colorsLight.orange,
    light: colorsLight.orangeLight,
    lighter: colorsLight.orangeLighter,
  },

  green: {
    darker: colorsLight.greenDarker,
    dark: colorsLight.greenDark,
    default: colorsLight.green,
    light: colorsLight.greenLight,
    lighter: colorsLight.greenLighter,
  },

  red: {
    darker: colorsLight.redDarker,
    dark: colorsLight.redDark,
    default: colorsLight.red,
    light: colorsLight.redLight,
    lighter: colorsLight.redLighter,
  },

  yellow: {
    darker: colorsLight.yellowDarker,
    dark: colorsLight.yellowDark,
    default: colorsLight.yellow,
    light: colorsLight.yellowLight,
    lighter: colorsLight.yellowLighter,
  },

  chart: {
    blue: colorsLight.chartBlue,
    yellow: colorsLight.chartYellow,
    red: colorsLight.chartRed,
    violet: colorsLight.chartViolet,
    green: colorsLight.chartGreen,
    pink: colorsLight.chartPink,
    navyLight: colorsLight.chartNavyLight,
    //navy: colorsLight.chartNavy,
  },
};
