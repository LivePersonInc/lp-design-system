import Icon, { IconProps } from '../Icon';

import ApplicationIntentBuilder from './icons/name=application-intent-builder.svg';

export type ApplicationIntentBuilderIconProps = IconProps

class ApplicationIntentBuilderIcon extends Icon {
  icons = ApplicationIntentBuilder;
}

export default ApplicationIntentBuilderIcon
