import { useEffect, useRef } from 'react';
import { Meta } from '@storybook/addon-docs';

import Preview from '@docs/components/Preview';
import ReadmeWrap from '@docs/components/ReadmeWrap';

import Readme from './readme.md';

<Meta title="Components/MultiselectChip" />

<ReadmeWrap>
  <Readme />
</ReadmeWrap>

## Basic

export const MultiselectChipTemplate = props => (
  <lp-multiselect-chip {...props}>
    {Array(5).fill(null).map((item, index) => <lp-chip key={index}>{`Chip ${++index}`}</lp-chip>)}
  </lp-multiselect-chip>
);

<Preview>
  <MultiselectChipTemplate />
</Preview>

<Preview light>
  <MultiselectChipTemplate theme="light" />
</Preview>

## Sizes

#### Default

<Preview>
  <MultiselectChipTemplate />
</Preview>

#### Small

<Preview>
  <MultiselectChipTemplate size="small" />
</Preview>
