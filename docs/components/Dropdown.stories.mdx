import { Meta, ArgsTable } from '@storybook/addon-docs';
import { styled } from '@storybook/theming';

import Dropdown from '@liveperson-design-system/components/src/components/dropdown/Dropdown';
import '@liveperson-design-system/components/build/dropdown';
import '@liveperson-design-system/components/build/button';

import Preview from '../ui/Preview';

<Meta title="Components/Dropdown" component={Dropdown}/>

export const DropdownContent = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  width: 200px;
  height: 100px;
  color: #fff;
  border-radius: ${({ theme }) => `${theme.appBorderRadius}px`};
  background-color: rgba(0, 0, 0, .7);
  overflow: auto;
`;

# Dropdown

```jsx
import '@liveperson-design-system/components/dropdown';
```

<ArgsTable of={Dropdown}/>

## Basic

export const BasicWrap = styled.div`
  min-height: 150px;
`;

<Preview>
  <BasicWrap>
    <lp-dropdown>
      <lp-button slot="toggle" label="Toggle" />
      <DropdownContent slot="content">Content</DropdownContent>
    </lp-dropdown>
  </BasicWrap>
</Preview>

## Auto content placement

export const AutoPlacementWrap = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  align-items: center;
  align-content: space-between;
  width: 100%;
  height: 100%;
  min-height: 400px;
  > div {
    display: flex;
    justify-content: flex-start;
    flex: 0 0 33%;
    &:nth-child(2),
    &:nth-child(3n + 2) {
      justify-content: center;
    }
    &:nth-child(3n) {
      justify-content: flex-end;
    }
  }
`;

<Preview>
  <AutoPlacementWrap id="auto-placement-wrap">
    {Array(9).fill(null).map((item, index) => (
      <div key={index}>
        <lp-dropdown
          parentSelector="#auto-placement-wrap"
          parentOffset={0}
        >
          <lp-button slot="toggle" label="Toggle"/>
          <DropdownContent slot="content">Content</DropdownContent>
        </lp-dropdown>
      </div>
    ))}
  </AutoPlacementWrap>
</Preview>
