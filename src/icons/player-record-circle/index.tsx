import { DireflowComponent } from 'direflow-component';

import PlayerRecordCircleIcon from './PlayerRecordCircleIcon';

DireflowComponent.create({
  component: PlayerRecordCircleIcon,
  configuration: {
    tagname: 'lp-player-record-circle-icon',
  },
});
