import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { getPropsByDefaultProps } from '../../common/helpers';
import { commonArgTypes } from '../../common/storybook';

import InputHelper from './InputHelper';
import './';

<Meta
  title="Components/Inputs/InputHelper"
  component={InputHelper}
  args={InputHelper.defaultProps}
  argTypes={commonArgTypes}
/>

export const Template = props => <lp-input-helper {...getPropsByDefaultProps(InputHelper.defaultProps, props)} />;

# InputHelper

<ArgsTable of={InputHelper} />

## Basic

<Canvas>
  <Story name="Basic">
    {Template.bind({})}
  </Story>
</Canvas>
