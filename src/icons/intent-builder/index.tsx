import { DireflowComponent } from 'direflow-component';

import IntentBuilderIcon from './IntentBuilderIcon';

DireflowComponent.create({
  component: IntentBuilderIcon,
  configuration: {
    tagname: 'lp-intent-builder-icon',
  },
});
