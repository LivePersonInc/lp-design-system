import { Meta } from '@storybook/addon-docs';

import * as animation from '@liveperson-design-system/tokens/js/animation';

import TokensList from './TokensList';

<Meta title="Tokens/Animation" />

# Animation

These are a set of animation values.

## How to use

### SCSS

To use keyframes animations provided by LPDS you have to also import `animation-keyframes.scss` file

```scss
@use '~@liveperson-design-system/tokens/scss/animation';

@import '~@liveperson-design-system/tokens/scss/animation-keyframes';

.button {
  animation: animation.$animation-slide-up-fade-in;
}
```

### JS

```js
import styled from 'styled-components';

import { variables, keyframes as LPDSLeyframes } from '@liveperson-design-system/tokens/js/animation';

const slideUpFadeIn = keyframes`
  ${LPDSLeyframes.slideUpFadeIn}
`;

const Button = styled.button`
  animation: ${slideUpFadeIn} ${variables.animationSetting};
`;
```

## Variables

The `$animation-setting` token provides a default value for use in all animations.

<TokensList
  header={['SCSS', 'JS', 'CSS', 'Values']}
  body={[
    [<code>$duration-fast</code>, <code>durations.fast</code>, <code>--lpds-animation-duration-fast</code>, animation.variables.durations.fast],
    [<code>$duration-normal</code>, <code>durations.normal</code>, <code>--lpds-animation-duration-normal</code>, animation.variables.durations.normal],
    [<code>$duration-slow</code>, <code>durations.slow</code>, <code>--lpds-animation-duration-slow</code>, animation.variables.durations.slow],
    [<code>$ease-in-quad</code>, <code>timingFunctions.easeInQuad</code>, <code>--lpds-animation-ease-in-quad</code>, animation.variables.timingFunctions.easeInQuad],
    [<code>$ease-out-quad</code>, <code>timingFunctions.easeOutQuad</code>, <code>--lpds-animation-ease-out-quad</code>, animation.variables.timingFunctions.easeOutQuad],
    [<code>$ease-in-out-quad</code>, <code>timingFunctions.easeInOutQuad</code>, <code>--lpds-animation-ease-in-out-quad</code>, animation.variables.timingFunctions.easeInOutQuad],
    [<code>$ease-in-cubic</code>, <code>timingFunctions.easeInCubic</code>, <code>--lpds-animation-ease-in-cubic</code>, animation.variables.timingFunctions.easeInCubic],
    [<code>$ease-out-cubic</code>, <code>timingFunctions.easeOutCubic</code>, <code>--lpds-animation-ease-out-cubic</code>, animation.variables.timingFunctions.easeOutCubic],
    [<code>$ease-in-out-cubic</code>, <code>timingFunctions.easeInOutCubic</code>, <code>--lpds-animation-ease-in-out-cubic</code>, animation.variables.timingFunctions.easeInOutCubic],
    [<code>$ease-in-quart</code>, <code>timingFunctions.easeInQuart</code>, <code>--lpds-animation-ease-in-quart</code>, animation.variables.timingFunctions.easeInQuart],
    [<code>$ease-out-quart</code>, <code>timingFunctions.easeOutQuart</code>, <code>--lpds-animation-ease-out-quart</code>, animation.variables.timingFunctions.easeOutQuart],
    [<code>$ease-in-out-quart</code>, <code>timingFunctions.easeInOutQuart</code>, <code>--lpds-animation-ease-in-out-quart</code>, animation.variables.timingFunctions.easeInOutQuart],
    [
      <code>$animation-setting</code>,
      <code>animationSetting</code>,
      <code>--lpds-animation-setting</code>,
      animation.variables.animationSetting.replace(
        `${animation.variables.durations.normal} ${animation.variables.timingFunctions.easeOutCubic}`,
        '$duration-normal $ease-out-cubic',
      ),
    ],
    [
      <code>$animation-slide-up-fade-in</code>,
      <code>animations.slideUpFadeIn</code>,
      <code>--lpds-animation-slide-up-fade-in</code>,
      animation.variables.animations.slideUpFadeIn.replace(animation.variables.animationSetting, '$animation-setting'),
    ],
    [
      <code>$animation-slide-up-fade-out</code>,
      <code>animations.slideUpFadeOut</code>,
      <code>--lpds-animation-slide-up-fade-out</code>,
      animation.variables.animations.slideUpFadeOut.replace(animation.variables.animationSetting, '$animation-setting'),
    ],
    [
      <code>$animation-slide-down-fade-in</code>,
      <code>animations.slideDownFadeIn</code>,
      <code>--lpds-animation-slide-down-fade-in</code>,
      animation.variables.animations.slideDownFadeIn.replace(animation.variables.animationSetting, '$animation-setting'),
    ],
    [
      <code>$animation-slide-down-fade-out</code>,
      <code>animations.slideDownFadeOut</code>,
      <code>--lpds-animation-slide-down-fade-out</code>,
      animation.variables.animations.slideDownFadeOut.replace(animation.variables.animationSetting, '$animation-setting'),
    ],
  ]}
/>

## CSS helpers classes

### Duration

To apply `animation-direction` style just add `lpds-animation-duration-{duration}` class to the element.

Available durations:

- `fast`
- `normal`
- `slow`

### Timing function

To apply `animation-timing-function` style just add `lpds-animation-timing-function-{timing-function}` class to the element.

Available timing functions:

- `ease-in-quad`
- `ease-out-quad`
- `ease-in-out-quad`
- `ease-in-cubic`
- `ease-out-cubic`
- `ease-in-out-cubic`
- `ease-in-quart`
- `ease-out-quart`
- `ease-in-out-quart`

### Animation setting

To apply `animation` style with default value to use in all animations just add `lpds-animation-setting` class to the element.

### Animation

To apply `animation` style just add `lpds-animation-{animation-name}` class to the element.

Available animations:

- `slide-up-fade-in`
- `slide-up-fade-out`
- `slide-down-fade-in`
- `slide-down-fade-out`
