import { createElement } from 'react';
import { Meta, Story, Canvas, ArgsTable, SourceState } from '@storybook/addon-docs';

import { icons } from 'lpds/components/icons/icons';

import 'lpds/components/icons';

import Button from './Button';

import './';

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    theme: {
      control: { type: 'inline-radio' },
    },
    size: {
      control: { type: 'inline-radio' },
    },
  }}
/>

# Button

<ArgsTable of={Button} />

`label` value can be provided by `label` attribute or by `slot` tag with the attribute `slot="label"`

Example:
```
<lp-button label="Button"></lp-button>
```
or
```
<lp-button>
  <slot slot="label">Button</slot>
</lp-button>
```

## Basic

<Canvas>
  <Story
    name="Basic"
    args={{
      theme: 'dark',
      variant: 'primary',
      size: 'medium',
      label: 'Button',
      disabled: false,
    }}
  >
    {({ theme, variant, size, label, disabled }) => (
      <lp-button
        theme={theme === 'dark' ? undefined : theme}
        variant={variant === 'primary' ? undefined : variant}
        size={size === 'medium' ? undefined : size}
        label={label}
        disabled={disabled === false ? undefined : disabled}
      />
    )}
  </Story>
</Canvas>

## With icon

You can add any LPDS icon component as child of Button component by using `slot` tag with `slot` attribute with value `icon-left` or `icon-right`

**Note:** Do not forget to import this icon component

<Canvas withSource={SourceState.OPEN}>
  <Story
    name="With icon"
    args={{
      icon: icons[0],
      iconPlacement: 'left',
      theme: 'dark',
      variant: 'primary',
      size: 'medium',
      label: 'With icon',
      disabled: false,
    }}
    argTypes={{
      theme: {
        table: { category: 'Button args' },
      },
      variant: {
        table: { category: 'Button args' },
      },
      size: {
        table: { category: 'Button args' },
      },
      label: {
        table: { category: 'Button args' },
      },
      icon: {
        options: icons,
        control: { type: 'select' },
      },
      iconPlacement: {
        options: ['left', 'right'],
        control: { type: 'inline-radio' },
      },
    }}
  >
    {({ icon, iconPlacement, theme, variant, size, label, disabled }) => (
      <lp-button
        key={`${icon}-${iconPlacement}`}
        theme={theme === 'dark' ? undefined : theme}
        variant={variant === 'primary' ? undefined : variant}
        size={size === 'medium' ? undefined : size}
        label={label}
        disabled={disabled === false ? undefined : disabled}
      >
        <slot slot={`icon-${iconPlacement}`}>
          {createElement(`lp-${icon}-icon`)}
        </slot>
      </lp-button>
    )}
  </Story>
</Canvas>
