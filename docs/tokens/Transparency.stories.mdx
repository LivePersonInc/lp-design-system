import { ColorItem, ColorPalette, Meta } from '@storybook/addon-docs';
import { styled } from '@storybook/theming';

import * as transparency from '@liveperson-design-system/tokens/build/js/transparency';

import VariablesList from '../ui/VariablesList.mdx';

import { getVariablesList } from './helpers.mdx';

<Meta title="Tokens/Transparency" />

# Transparency

export const DarkColorItem = styled.div`
  > div {
    > div:last-of-type {
      > div {
        > div:first-of-type {
          background-color: #fff;
        }
      }
    }
  }
`;

<ColorPalette>
  <ColorItem title="Light" subtitle="" colors={transparency.variables.light} />
  <DarkColorItem>
    <ColorItem title="Dark" subtitle="" colors={transparency.variables.dark} />
  </DarkColorItem>
</ColorPalette>

<VariablesList
  variables={[
    [<code>$disabled</code>, <code>disabled</code>, <code>--lpds-transparency-disabled</code>, transparency.variables.disabled],
    ...getVariablesList(transparency.variables.light, ['$light-', 'light.', '--lpds-transparency-light-']),
    ...getVariablesList(transparency.variables.dark, ['$dark-', 'dark.', '--lpds-transparency-dark-']),
  ]}
/>

## CSS helpers classes

### Disabled

To apply `opacity` style for disabled elements just add `lpds-transparency-disabled` class.

### Background color

To apply `background-color` style just add `lpds-transparency-{light|dark}-{0|1|2|3|4}` class to the element.
