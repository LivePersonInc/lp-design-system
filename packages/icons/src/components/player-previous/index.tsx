import { DireflowComponent } from 'direflow-component';

import PlayerPreviousIcon from './PlayerPreviousIcon';

DireflowComponent.create({
  component: PlayerPreviousIcon,
  configuration: {
    tagname: 'lp-player-previous-icon',
  },
});
